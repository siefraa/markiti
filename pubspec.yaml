name: twende_markiti
description: Online Market Shopping app with admin permissions system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI Components
  cupertino_icons: ^1.0.6
  
  # State Management (optional - can add provider, riverpod, bloc, etc)
  # provider: ^6.1.1
  
  # Network & API
  # http: ^1.1.0
  # dio: ^5.4.0
  
  # Local Storage
  # shared_preferences: ^2.2.2
  # hive: ^2.2.3
  # hive_flutter: ^1.1.0
  
  # Image Handling
  # image_picker: ^1.0.7
  # cached_network_image: ^3.3.1
  
  # Authentication
  # firebase_auth: ^4.16.0
  # firebase_core: ^2.24.2
  
  # Payment Integration (M-Pesa)
  # mpesa_flutter_plugin: ^1.3.0
  
  # Utils
  # intl: ^0.19.0
  # uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  # Add assets
  # assets:
  #   - assets/images/
  #   - assets/icons/
  
  # Add fonts
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #       - asset: fonts/Roboto-Bold.ttf
  #         weight: 700
